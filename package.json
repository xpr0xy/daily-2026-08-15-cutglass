{
  "name": "cutglass",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite --host 127.0.0.1",
    "build": "vite build && node scripts/copy-build.js",
    "test": "node --test tests/*.test.js",
    "validate:wav": "node scripts/validate-wav.js"
  },
  "devDependencies": {
    "@playwright/test": "^1.55.0",
    "vite": "^7.1.2"
  }
}
